/// <reference types="react" />
import { CSSStyle, Theme } from "../types";
export interface GlobalTheme {
    theme: Theme;
}
/**
 * 创建全局样式
 * @param styles
 */
export declare const createGlobalStyle: (...styles: CSSStyle<GlobalTheme>) => import("react").NamedExoticComponent<object>;
