import { GlobalTheme } from "../../functions";
import { CSSStyle } from "../../types";
import { FC } from 'react';
export interface GlobalStyleProps {
    /**
     * 全局样式
     */
    globalStyle?: CSSStyle<GlobalTheme>;
}
export declare const GlobalStyle: FC<GlobalStyleProps>;
