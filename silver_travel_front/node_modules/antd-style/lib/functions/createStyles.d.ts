import { type CSSObject } from '@emotion/css';
import type { Emotion } from '@emotion/css/create-instance';
import { FullToken, Theme } from "../types";
import type { AntdStylish } from "../stylish";
export interface CreateStylesTheme {
    token: FullToken;
    stylish: AntdStylish;
}
export interface ReturnStyles<Key extends string> {
    styles: Record<Key, string>;
    theme: Theme;
    cx: Emotion['cx'];
}
export declare type StyleParams<T extends string> = string | Record<T, CSSObject | string>;
/**
 * 业务应用中创建样式基础写法
 */
export declare function createStyles<Props, Key extends string>(cssStyleOrGetCssStyleFn: StyleParams<Key> | ((theme: CreateStylesTheme, props?: Props) => StyleParams<Key>)): (props?: Props) => ReturnStyles<Key>;
